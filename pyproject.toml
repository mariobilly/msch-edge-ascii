[project]
name = "msch-edge-ascii"
version = "0.1.0"
description = "Contour-aligned ASCII strokes, halftone fills and animated marks for photos and native ComfyUI video."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = ["numpy", "Pillow", "torch", "av"]

[project.urls]
Repository = "https://github.com/mariobilly/msch-edge-ascii"
Documentation = "https://github.com/mariobilly/msch-edge-ascii/blob/main/docs/NODES.md"
"Bug Tracker" = "https://github.com/mariobilly/msch-edge-ascii/issues"

[tool.comfy]
PublisherId = "mariobilly"
DisplayName = "MSCH Edge ASCII"

